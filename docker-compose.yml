version: "3.8"

services:
  # ── PostgreSQL ───────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: netraassist
      POSTGRES_USER: netraassist
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    volumes:
      - pgdata:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 4G
    command: postgres -c shared_buffers=4GB -c work_mem=32MB -c maintenance_work_mem=256MB -c effective_cache_size=16GB -c max_connections=200 -c checkpoint_completion_target=0.9 -c wal_buffers=64MB -c random_page_cost=1.1
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U netraassist"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Redis ────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    deploy:
      resources:
        limits:
          memory: 384M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Flask/Gunicorn (API + Frontend) ─────────────────────────────────
  web:
    build:
      context: .
      args:
        VITE_MS_SSO_CLIENT_ID: ${VITE_MS_SSO_CLIENT_ID}
        VITE_MS_SSO_TENANT_ID: ${VITE_MS_SSO_TENANT_ID}
        VITE_MS_SSO_REDIRECT: ${VITE_MS_SSO_REDIRECT}
    restart: unless-stopped
    ports:
      - "${PORT:-3002}:${PORT:-3002}"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://netraassist:${POSTGRES_PASSWORD:-changeme}@db:5432/netraassist
      REDIS_URL: redis://redis:6379/0
      GUNICORN_WORKERS: ${GUNICORN_WORKERS:-9}
      GUNICORN_THREADS: ${GUNICORN_THREADS:-4}
    volumes:
      - uploads:/app/backend/uploads
      - downloads:/app/backend/downloads
    deploy:
      resources:
        limits:
          memory: 6G
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ── Celery Worker ───────────────────────────────────────────────────
  celery:
    build: .
    restart: unless-stopped
    command: >
      celery -A backend.celery_app worker
      --queues=batch
      --concurrency=${DOC_PROCESS_MAX_WORKERS:-8}
      --loglevel=info
      --without-heartbeat
      --without-mingle
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://netraassist:${POSTGRES_PASSWORD:-changeme}@db:5432/netraassist
      REDIS_URL: redis://redis:6379/0
    volumes:
      - uploads:/app/backend/uploads
      - downloads:/app/backend/downloads
    deploy:
      resources:
        limits:
          memory: 6G
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pgdata:
  uploads:
  downloads:
